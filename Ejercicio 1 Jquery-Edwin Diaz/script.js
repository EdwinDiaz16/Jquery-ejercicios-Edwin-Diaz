$(document).ready(function() {
    $('div.module');
    $(':hidden');
    $(':odd');
    
    if ($('div.module'));
    var $divs = $("div.module");
    console.log($divs);
  
    if ($(':hidden').length);
    var $hidden = $(":hidden");
    console.log($hidden);
  
    if ($(':odd').select);
    var $odd = $(':odd');
    console.log($odd);
    
    });